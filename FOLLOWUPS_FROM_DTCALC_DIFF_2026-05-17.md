# Follow-ups surfaced from dtcalc-sun vs dtcalc-swiftx diff (2026-05-17)

Generated by `swiftx-capture diff captures/dtcalc-sun.xtap captures/dtcalc-swiftx.xtap --only-different`. These are NOT in scope for `PLAN_PIXMAP_AND_COPYAREA.md` but came out of the same investigation. Don't lose them.

## 1. QueryFont returns empty char table for substituted fonts

Sun: `QueryFont ascent/descent=11/2 chars=224 properties=21`
Us:  `QueryFont ascent/descent=10/3 chars=0   properties=0`

When Motif requests `-dt-interface user-medium-r-normal-...` we substitute `Fixed` and return what looks like a stub-empty font reply. Motif can't compute widget text widths from chars=0 and falls back to defaults — almost certainly the upstream cause of widget-geometry divergence (see #2). Find the QueryFont handler; on substitution, populate the char table from the substituted CTFont's actual metrics instead of returning empty.

## 2. Expose-vs-ConfigureNotify cascade divergence in early widget packing

Sun S2C around lines 102–110 of the diff: clean `ConfigureNotify 143x{46,67,69,90,111,134,...}` cascade as Motif's container manager packs each row in.
Us S2C: mostly `Expose` events on the same window, plus only one `ConfigureNotify`. Likely a downstream symptom of #1 (Motif on us doesn't know widget sizes, so the packing math returns different / fewer ConfigureWindow calls), or possibly we're not emitting ConfigureNotify on geometry changes we should be (real bug, separate from #1). Verify after fixing #1 whether this resolves on its own. Possibly the upstream cause of the "xcalc resize moves text but not buttons" symptom.

## 3. SolarisIA extension missing

Sun: `QueryExtension(SolarisIA) present=true major=135`. Us: present=false.

SolarisIA is the Solaris-internal Inter-client Atoms cache. When absent Motif falls back to interning ~50 atoms one-by-one at startup (diff lines 19–61). No visual bug — just latency and log noise. Real fix: implement a minimal SolarisIA extension that just batches atom interning. Cheap (~half day).

## 4. CDE atoms not pre-interned at server startup

Cosmetic: clients see different atom IDs vs Sun's pre-allocated set, but resource handles still work. Pre-intern the standard CDE/Motif atom set in `AtomTable.init()` (the 50 atoms in #3 plus the `_MOTIF_*` family). Mostly buys quiet logs and slightly better diff readability. ~1 hour.

## Diff tool wart

Tool aligns C2S/S2C by sequence number, which means any XID-allocation offset propagates downstream and ~90% of "different" rows are really structural-offset noise. Useful improvement someday: normalize on XID-equivalence before diffing. Not blocking.
